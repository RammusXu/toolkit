[tool.poetry]
name = "python"
version = "0.1.0"
description = ""
authors = ["Rammus Xu <comte_ken@hotmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"
python-dateutil = "^2.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
